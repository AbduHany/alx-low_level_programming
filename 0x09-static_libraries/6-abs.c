#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer.
 * @n: the integer that's converted to absolute value.
 *
 * Return: it returns the absolute integer value of n.
 */
int _abs(int n)
{
	return (abs(n));
}
