#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string, followed by a new line
 * @str: the string that is being printed to stdout.
 *
 * Return: Void.
 */
void _puts(char *str)
{
	puts(str);
}
